# Kubernetes Installation

Requirements:
* CPU core number >= 2
* Memory >= 2GB
* Close swap and selinux

```shell
apt update
apt full-upgrade -y
apt autoremove
apt autoclean

# close swap
swapoff -a

```