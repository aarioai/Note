> 专注于少数核心问题，并能说出它需要花费多长时间或成本。
> 索引应当在系统投产后不久就被固定下来，而不再需要大量的实验。

* 索引会消耗磁盘，会导致写操作（INSERT, UPDATE, DELETE）变慢。

假设只有根页在内存中，即使所有非叶子页都在数据库缓冲池中，每个索引层级也将向索引扫描过程贡献约50微秒(μs)的中央处理单元(CPU)时间。如果非叶子页不在缓冲池中，而在磁盘服务器的读缓存中，那么读取索引页可能会花费1毫秒(ms)。而一次磁盘随机读取需要大约10ms.